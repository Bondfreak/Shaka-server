# F1-T05 — Asset Instance bootstrap (BB/SB)

Bootstrap vessel **Asset Instances** for M/Y Shaka twin propulsion with **Unknown-safe fields**. No Foundation Drive writes. No LLM. Never invent serials; never merge BB/SB into one Asset.

## Layout

- `src/shaka_server/f1/assets/bootstrap.py` — `bootstrap_shaka_assets()`
- `src/shaka_server/f1/assets/registry.py` — `AssetRegistry` (list by side / type / ko_id)
- `src/shaka_server/f1/assets/api.py` — thin `get_asset(id)`, `list_assets(side=...)`
- Snapshot integration: `build_ac_fixture_snapshot()` loads bootstrap assets so retrieval/composer resolve Asset IDs
- Tests: `tests/test_f1_assets.py`

## Assets created

| ID | Type | Side | Serial | KO |
|----|------|------|--------|-----|
| `AI-SHAKA-VESSEL` | vessel | unknown | unknown | — |
| `AI-D4-BB-ENGINE` | engine | BB | `2004030432` (documented) | `KO-D4-0062` |
| `AI-D4-SB-ENGINE` | engine | SB | `2004030433` (documented) | `KO-D4-0062` |
| `AI-D4-BB-IPS` | ips | BB | unknown | `KO-D4-0103` |
| `AI-D4-SB-IPS` | ips | SB | unknown | `KO-D4-0103` |
| `AI-D4-BB-PCU` | pcu | BB | unknown (CF-003) | `KO-D4-0190` |
| `AI-D4-SB-PCU` | pcu | SB | unknown (placeholder) | `KO-D4-0190` |
| `AI-D4-IMPELLER-KIT` | impeller | unknown | unknown | `KO-D4-0007` |
| `AI-D4-IMPELLER-CANDIDATE-A/B` | impeller | unknown | unknown | `KO-D4-0007` |

Vessel location notes **NW370 Fly 2009**. Impeller candidates keep side **unknown** (CF-002: invoice ×2 ≠ BB/SB install split).

## Invariants

- Serial helper: blank / absent → `"unknown"`; refuses guesses
- `Side.BB` and `Side.SB` never share one Asset id
- Bootstrap is **idempotent** (same IDs on repeat)
- KO ≠ Asset; link `ko_id` only where known

## API

```python
from shaka_server.f1.assets import bootstrap_shaka_assets, get_asset, list_assets

assets = bootstrap_shaka_assets()
bb_engines = list_assets(side="BB", asset_type="engine")
pcu = get_asset("AI-D4-BB-PCU")
```
